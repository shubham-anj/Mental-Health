<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log in as user</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Custom CSS for this specific login page */
        body {
            background-color: #2c3e50; /* Dark background as in image */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh; /* Full viewport height */
            margin: 0;
            font-family: sans-serif;
        }

        .login-container {
            background-color: #34495e; /* Slightly lighter dark background for the card */
            border-radius: 10px;
            overflow: hidden; /* Ensures inner elements respect border-radius */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5); /* Subtle shadow */
            max-width: 900px; /* Adjust as needed */
            width: 90%; /* Responsive width */
        }

        .left-panel {
            padding: 40px;
            color: #ecf0f1; /* Light text color */
        }

        .left-panel h2 {
            font-size: 2.2rem;
            margin-bottom: 30px;
            font-weight: 700;
            color: #ecf0f1;
        }

        /* Form input styling */
        .form-control-custom {
            background-color: #4a647d; /* Darker input field background */
            border: none;
            color: #ecf0f1; /* Input text color */
            padding: 15px 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .form-control-custom::placeholder {
            color: rgba(236, 240, 241, 0.7); /* Lighter placeholder text */
        }

        .form-control-custom:focus {
            background-color: #5d7a96; /* Slightly lighter on focus */
            color: #ecf0f1;
            box-shadow: 0 0 0 0.25rem rgba(85, 168, 167, 0.25); /* Teal focus ring */
            border-color: #55a8a7; /* Teal border on focus */
        }

        /* Password input group specific styling */
        .password-input-group .input-group-text {
            background-color: #4a647d;
            border: none;
            color: rgba(236, 240, 241, 0.7);
            border-top-right-radius: 8px;
            border-bottom-right-radius: 8px;
            cursor: pointer;
        }

        .password-input-group .form-control-custom {
            margin-bottom: 0; /* Remove bottom margin for input in group */
        }

        .forgot-password {
            text-align: right;
            margin-bottom: 30px;
            font-size: 0.9rem;
        }

        .forgot-password a {
            color: #8bbcdc; /* Light blue for link */
            text-decoration: none;
        }

        .forgot-password a:hover {
            color: #55a8a7; /* Teal on hover */
        }

        /* Log In button styling */
        .btn-login {
            background-color: #1a4a58; /* Dark teal button */
            color: white;
            padding: 12px 0;
            border-radius: 8px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: background-color 0.3s ease;
            width: 100%; /* Full width button */
            border: none;
        }

        .btn-login:hover {
            background-color: #266b80; /* Lighter teal on hover */
            color: white;
        }

        /* OR separator */
        .or-separator {
            display: flex;
            align-items: center;
            text-align: center;
            margin: 30px 0;
            color: #95a5a6; /* Grey text */
        }

        .or-separator::before,
        .or-separator::after {
            content: '';
            flex: 1;
            border-bottom: 1px solid #7f8c8d; /* Darker grey line */
        }

        .or-separator:not(:empty)::before {
            margin-right: .5em;
        }

        .or-separator:not(:empty)::after {
            margin-left: .5em;
        }

        /* Social login buttons */
        .social-login-btn {
            background-color: #4a647d; /* Same as input background */
            border: none;
            border-radius: 8px;
            padding: 10px 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background-color 0.3s ease;
        }

        .social-login-btn:hover {
            background-color: #5d7a96; /* Lighter on hover */
        }

        .social-login-btn img {
            height: 24px;
            margin-right: 10px;
        }

        .social-login-btn span {
            color: #ecf0f1;
            font-weight: 600;
        }
        
        .continue-with-text {
            color: #95a5a6;
            font-size: 0.85rem;
            margin-top: 15px;
        }


        /* Right panel styling */
        .right-panel {
            background-color: #1a4a58; /* Dark teal background */
            color: white;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .right-panel h2 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            color: white;
        }

        .right-panel p {
            font-size: 1.1rem;
            line-height: 1.6;
            color: rgba(255, 255, 255, 0.8);
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .login-container {
                width: 95%;
                margin: 20px;
            }
            .right-panel {
                display: none; /* Hide right panel on smaller screens */
            }
            .left-panel {
                border-radius: 10px; /* Adjust border radius if right panel is hidden */
            }
        }
    </style>
</head>
<body>

    <div class="login-container row g-0">
        <div class="col-lg-7 left-panel">
            <h2 class="mb-4">Log in as user</h2>
            <form action="/user/userPage">
                <div class="mb-3">
                    <input type="text" name="name" class="form-control form-control-custom" id="name" placeholder="Enter your full name here" required>
                </div>
                <div class="mb-3">
                    <input type="email" name="email" class="form-control form-control-custom" id="emailInput" placeholder="Email" required>
                </div>
                <div class="input-group password-input-group mb-3">
                    <input type="password" name="password" class="form-control form-control-custom" id="passwordInput" placeholder="Password" required>
                    <span class="input-group-text toggle-password" id="togglePassword">
                        <i class="fas fa-eye"></i>
                    </span>
                </div>
                <div class="forgot-password">
                    <a href="#">Forgot Password?</a>
                </div>
                <button type="submit" class="btn btn-login mb-4">Log In</button>
            </form>

            <div class="or-separator">OR</div>

            <div class="d-flex justify-content-center gap-3 mt-4">
                <a href="#" class="btn social-login-btn flex-grow-1">
                    <img src="https://img.icons8.com/color/48/000000/google-logo.png" alt="Google">
                    </a>
                <a href="#" class="btn social-login-btn flex-grow-1">
                    <img src="https://img.icons8.com/color/48/000000/linkedin.png" alt="LinkedIn">
                    </a>
            </div>
            <p class="text-center continue-with-text">continue with...</p>
        </div>

        <div class="col-lg-5 right-panel">
            <h2>Welcome back!</h2>
            <p>Log in to access your dashboard...</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Password visibility toggle
        const togglePassword = document.getElementById('togglePassword');
        const passwordInput = document.getElementById('passwordInput');

        togglePassword.addEventListener('click', function () {
            const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
            passwordInput.setAttribute('type', type);
            this.querySelector('i').classList.toggle('fa-eye');
            this.querySelector('i').classList.toggle('fa-eye-slash');
        });
    </script>
</body>
</html>