<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Community & Support | Aura Wellness</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    body {
      background-color: #2c3e50;
      font-family: "Segoe UI", sans-serif;
      color: #ecf0f1;
      min-height: 100vh;
    }

    .navbar {
      background-color: #34495e;
      border-bottom: 2px solid #55a8a7;
    }

    .navbar-brand {
      font-weight: 600;
      color: #55a8a7 !important;
    }

    .container {
      max-width: 900px;
      margin-top: 50px;
    }

    .group-card {
      background-color: #3e5267;
      border: 1px solid #51667f;
      border-radius: 12px;
      padding: 20px;
      margin-bottom: 15px;
      transition: all 0.2s ease-in-out;
    }

    .group-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 0 12px rgba(85, 168, 167, 0.3);
    }

    .btn-join {
      background-color: #55a8a7;
      color: #fff;
      border: none;
    }

    .btn-join:hover {
      background-color: #469b9a;
    }

    .no-groups {
      text-align: center;
      color: #95a5a6;
      font-style: italic;
      margin-top: 40px;
    }

    /* Chat Styles */
    .chat-container {
      background-color: #2e3b4e;
      padding: 15px;
      border-radius: 10px;
      border: 1px solid #51667f;
      margin-top: 10px;
    }
    .chat-message {
      background-color: #3e5267;
      padding: 8px 12px;
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="#"><i class="fa-solid fa-leaf me-2"></i>Aura Wellness</a>
      <div class="d-flex">
        <a href="chatbot.html" class="btn btn-join me-2"><i class="fa-solid fa-comments"></i> Chatbot</a>
        <a href="appointments.html" class="btn btn-join"><i class="fa-solid fa-calendar-check"></i> Book Session</a>
      </div>
    </div>
  </nav>

  <!-- Community Groups -->
  <div class="container">
    <h3 class="text-center mb-4"><i class="fa-solid fa-users me-2"></i>Anonymous Support Groups</h3>

    <div id="groupList">
      <!-- Stress Management with expandable chat -->
      <div class="group-card">
        <div class="d-flex justify-content-between align-items-center">
          <div>
            <h5>Stress Management</h5>
            <small>Moderated group for stress relief and coping tips</small>
          </div>
          <button class="btn btn-join" onclick="toggleChat('stressChat')">
            <i class="fa-solid fa-right-to-bracket me-1"></i> Join
          </button>
        </div>

        <!-- Hidden chat container -->
        <div id="stressChat" class="chat-container" style="display:none;">
          <div class="chat-message mb-2">
            <strong>Alex:</strong> Hey everyone! How are you feeling today?
          </div>
          <div class="chat-message mb-2 text-end">
            <strong>You:</strong> A bit stressed, hoping to relax here.
          </div>
          <div class="chat-message mb-2">
            <strong>Sam:</strong> Same here, this group is really helpful 🌱
          </div>
          <div class="mt-2">
            <input type="text" class="form-control" placeholder="Type a message..." disabled>
            <small class="text-muted">Chat is demo only</small>
          </div>
        </div>
      </div>

      <!-- Other placeholder groups -->
      <div class="group-card d-flex justify-content-between align-items-center">
        <div>
          <h5>Depression Support</h5>
          <small>Share experiences and get peer support anonymously</small>
        </div>
        <button class="btn btn-join">
          <i class="fa-solid fa-right-to-bracket me-1"></i> Join
        </button>
      </div>

      <div class="group-card d-flex justify-content-between align-items-center">
        <div>
          <h5>Anxiety Relief</h5>
          <small>Group for discussing coping mechanisms and tips</small>
        </div>
        <button class="btn btn-join">
          <i class="fa-solid fa-right-to-bracket me-1"></i> Join
        </button>
      </div>
    </div>

    <div id="noGroups" class="no-groups" style="display:none;">
      No support groups available at the moment 🌱
    </div>
  </div>

  <script>
    function toggleChat(id) {
      const chat = document.getElementById(id);
      chat.style.display = chat.style.display === "none" ? "block" : "none";
    }
  </script>
</body>
</html>
